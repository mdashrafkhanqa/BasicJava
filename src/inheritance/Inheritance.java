package inheritance;

public class Inheritance extends Teacher {

    /* Inheritance can be defined as the process where one class extends the properties
       of another class, like a parent-child relationship. */

    public static void main(String[] args) {
        Inheritance obj = new Inheritance();
        obj.does();
        obj.eat();
    }


}
